// Flow: get the total balance of the room's treasury token held by our agent principal,
// get the room's total contributed amount(it's assumed that the room's treasury token is the same as the contributed token)
// calculate the total room contribution / total balance in our agent canister * 100 to get room's share
// get total value(in usd) of the treasury token in the agent canister and multiply by room's share to get room's earnings
